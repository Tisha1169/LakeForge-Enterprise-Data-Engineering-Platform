# pipelines/silver/

Thin orchestration around the PySpark jobs in [`spark/`](../../spark/README.md)
— see [pipelines/README.md](../README.md) for `runner.py`/`reader.py`, and
`spark/README.md` for the actual transformation logic.
