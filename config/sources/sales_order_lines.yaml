name: sales_order_lines
source_type: db
description: Order line items extracted from the operational Sales Postgres DB (needed for Sales fact-table grain).
schedule: "@daily"
schema_name: sales
table: order_lines
retry:
  max_attempts: 3
  initial_backoff_seconds: 2.0
  max_backoff_seconds: 20.0
